# gcovr config: run from the build directory with: gcovr -r ..
# This restricts coverage to project source only (excludes googletest, etc.)

filter = ^src/
filter = ^include/
exclude = ^tests/
exclude = ^libs/
exclude = ^build/
